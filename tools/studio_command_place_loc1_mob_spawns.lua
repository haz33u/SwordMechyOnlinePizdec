--[[
	STUDIO COMMAND BAR (Edit Mode) — paste & run once, then Save place.

	Legacy entrypoint → same Loc1 layout as studio_loc1_spawns_v2.lua
	Prefer tools/studio_loc1_spawns_v2.lua (this file stays for old docs links).
]]

-- Run v2 if present in the same folder context; otherwise inline minimal rings.
-- When pasting, use studio_loc1_spawns_v2.lua contents.
print("[studio_command_place] Use tools/studio_loc1_spawns_v2.lua — full Loc1 4-mob + boss marker script.")
print("Open that file, copy all, paste into View → Command Bar, Enter, then Save place.")
